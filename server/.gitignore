# 1. Dependencies
# Never push these; the pipeline will install them fresh using 'npm ci'
node_modules/

# 2. Environment Variables & Secrets
# This contains your Database URL and JWT Secrets. EXTREMELY IMPORTANT!
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. Logs
# These are generated when things crash; they don't belong in the repo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# 4. Build and Dist
# If you use TypeScript or a bundler later, ignore the output
dist/
build/

# 5. OS Specific Files
.DS_Store
Thumbs.db

# 6. Coverage
# If you run tests later (Week 3), ignore the coverage reports
coverage/